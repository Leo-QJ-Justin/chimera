"""Project package.

The scaffold renames this directory wholesale. Every import inside the
package is relative (``from ..core import ...``), so the rename touches
only the directory name and the literal ``PROJECT`` in run_*.py, tests/
and pyproject.toml.
"""

__version__ = "0.1.0"
