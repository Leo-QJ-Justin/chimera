# Pressure scenarios

One scenario per landed change. Each is walked before its change ships:
give a fresh session **only the edited file** — not the spec, not the
pass condition — present the Setup, then apply both pressures. The walk
passes when the rationalization has no compliant form in the text the
agent actually loads.

Shape: title → `**Edited surfaces:**` + `**Failure form:**` → `## Setup`
→ `## Failure to reproduce (without the edit)` → `## Pass condition` →
`## Pressure (two stacked)` → `## Walk result`, ending `PASS.`

## Index

`change-01` to `change-13` are keyed to the **v2 spec's** change
numbers (`docs/specs/2026-09-01-v2-improvements.md`). There is no
`change-10`: that change created this directory.

`change-14` to `change-22` are keyed to the **v1.10 improvements**, A to
I (`docs/specs/2026-09-12-v1.10-improvements.md`). Their numbers are
positions in this directory, not source-document change numbers — the
source proposals they absorb are named in each file's header. Two
numbers are easy to misread: `change-17` is improvement C, not the
held-back documentation skill; `change-18` is improvement A, not the
rejected genesis phase.

| File | Change / improvement | Surface |
|---|---|---|
| `change-01` | v2 Ch. 1 — Decisions section (+ `supersedes`, v1.10 D) | designing-tasks |
| `change-02` | v2 Ch. 2 — data-contact spike | design-project |
| `change-03` | v2 Ch. 3 — notation at first use | writing-in-ste |
| `change-04` | v2 Ch. 4 — amendment path | finishing-a-branch |
| `change-05` | v2 Ch. 5 — deviations | writing-plans, finishing-a-branch |
| `change-06` | v2 Ch. 6 — flow sketch | designing-tasks |
| `change-07` | v2 Ch. 7 — asked, not recorded | designing-tasks |
| `change-08` | v2 Ch. 8 — BIND phase | design-project |
| `change-09` | v2 Ch. 9 — evidence closure | exploring-reproducibly |
| `change-11` | v2 Ch. 11 — cross-reference checklist | design-project |
| `change-12` | v2 Ch. 12 — /retrospect | retrospect |
| `change-13` | v2 Ch. 13 — process map | process-map |
| `change-14` | **v1.10 E** — preconditions before task 1; splits | writing-plans, start-task, finishing-a-branch |
| `change-15` | **v1.10 B** — claims name their ground | designing-tasks, writing-plans |
| `change-16` | **v1.10 F** — a row names its outcome | design-project, start-task |
| `change-17` | **v1.10 C** — form, and aggregates ship instances | designing-tasks, writing-plans, verifying-before-done |
| `change-18` | **v1.10 A** — approve against cases | designing-tasks, writing-plans |
| `change-19` | **v1.10 G** — the acceptance question | persistent-model-discovery, design-project, PRD templates |
| `change-20` | **v1.10 I** — the survey guard | writing-comparative-reports |
| `change-21` | **v1.10 D** — decisions get a home | finishing-a-branch, designing-tasks |
| `change-22` | **v1.10 H** — the mutation check (experiment) | code-reviewer |
| `change-23` | **bootstrap** — the routing surface is keyword-matched | using-chimera, budget checker, session-start test |
